% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seurat_ext.R
\name{RunHitype}
\alias{RunHitype}
\alias{RunHitype.default}
\alias{RunHitype.Seurat}
\title{Run hitype_assign for a Seurat object}
\usage{
RunHitype(object, ...)

\method{RunHitype}{default}(object, ...)

\method{RunHitype}{Seurat}(
  object,
  gs = NULL,
  fallback = "Unknown",
  threshold = NULL,
  level_weights = function(l) 1/(10^(l - 1)),
  make_unique = FALSE,
  layer = "data",
  assay = NULL,
  scaled = FALSE,
  ident = NULL,
  ...
)
}
\arguments{
\item{object}{Seurat object}

\item{...}{Additional arguments passed to the specific method.}

\item{gs}{The gene list prepared by \code{\link{gs_prepare}}}

\item{fallback}{A fallback cell type if no cell type is assigned}

\item{threshold}{Confidence threshold as top1/top2 score ratio,
passed to \code{\link[=hitype_assign]{hitype_assign()}}. \code{NULL} (default) means no filtering.}

\item{level_weights}{The weights for each level of the hierarchy to calculate
the final cell type score
It should be either a numeric vector of length equal to the number of levels
or a single numeric value to be used for all levels
It can also be a function that takes the levels as input and returns a
numeric vectors as the weights.}

\item{make_unique}{Whether to make the cell type names unique}

\item{layer}{The layer to use for \code{GetAssayData}}

\item{assay}{The assay to use for \code{GetAssayData}}

\item{scaled}{Whether the data from \code{GetAssayData} is scaled}

\item{ident}{The identity column to use majority voting to assign cell types to clusters
If NULL, return cell-level assignments for each cell.
If "ident", return cluster(identity)-level assignments for each cluster.
if a character, return cluster-level assignments for each cluster based on the specified column in the metadata.}
}
\value{
The Seurat object with the cell types (named \code{hitype}) added to the
metadata
}
\description{
Run hitype_assign for a Seurat object
}
