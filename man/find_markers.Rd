% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_markers.R
\name{find_markers}
\alias{find_markers}
\title{Find marker genes for cell types}
\usage{
find_markers(
  exprs,
  clusters = NULL,
  method = c("fc", "seurat", "presto"),
  top = 20,
  min_log2fc = 0.25,
  min_pct = 0.1,
  only_pos = TRUE,
  include_negative = FALSE,
  level = 1
)
}
\arguments{
\item{exprs}{A genes x cells expression matrix (plain matrix or
dgCMatrix/dgTMatrix) or a Seurat object. For \code{method = "fc"}, the
input is expected to be log-normalized.}

\item{clusters}{A named vector of cluster ids (names = cells) or a
factor. If \code{exprs} is a Seurat object, defaults to
\code{Seurat::Idents(exprs)}.}

\item{method}{The marker-finding method. One of:
\describe{
\item{"fc"}{Fold-change based (default). No extra dependencies.
Ranks genes by \code{log2fc * (pct_in - pct_out)}.}
\item{"seurat"}{Seurat's \code{FindAllMarkers}. Requires a Seurat
object.}
\item{"presto"}{presto's Wilcoxon rank-sum test (\code{wilcoxauc}).
Requires the presto package.}
}}

\item{top}{Number of markers to return per cell type.}

\item{min_log2fc}{Minimum log2 fold change for a gene to be kept as a
marker (when \code{only_pos = TRUE}).}

\item{min_pct}{Minimum fraction of cells in the cell type expressing
the gene.}

\item{only_pos}{Only keep genes that are higher in the cell type than
in the rest of the cells (positive markers). If \code{FALSE}, all genes
passing \code{min_pct} are considered, ranked by score.}

\item{include_negative}{If \code{TRUE}, also fill the \code{geneSymbolmore2}
column with the top down-regulated markers per cell type.}

\item{level}{The hierarchy level to write into the output database.}
}
\value{
A data frame in the hitype db format with columns \code{cellName},
\code{geneSymbolmore1}, \code{geneSymbolmore2} and \code{level}, directly consumable
by \code{\link[=gs_prepare]{gs_prepare()}}.
}
\description{
Discovers marker genes for each cell type (cluster) in an expression
matrix or Seurat object, and returns them in the hitype marker database
format so the result can be passed directly to \code{\link[=gs_prepare]{gs_prepare()}}. Three
backends are available: the dependency-light fold-change method
(default), Seurat's \code{FindAllMarkers}, and presto's Wilcoxon rank-sum
test. For \code{method = "fc"}, the input expression matrix is expected to
be log-normalized.
}
\examples{
set.seed(1)
ngenes <- 40
ncells <- 60
exprs <- matrix(runif(ngenes * ncells, 0, 0.2), ngenes, ncells)
rownames(exprs) <- paste0("gene", seq_len(ngenes))
colnames(exprs) <- paste0("cell", seq_len(ncells))
clusters <- setNames(
    rep(c("Tcell", "Bcell", "Monocyte"), each = 20),
    colnames(exprs)
)
markers <- find_markers(exprs, clusters, method = "fc", top = 5)
head(markers)
}
