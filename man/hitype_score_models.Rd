% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hitype_score_models.R
\name{hitype_score_models}
\alias{hitype_score_models}
\title{Score cells with the models trained by train_weights()}
\usage{
hitype_score_models(exprs, models, margin = 0)
}
\arguments{
\item{exprs}{Input scRNA-seq expression matrix (genes x cells, the same
convention as \code{\link[=hitype_score]{hitype_score()}}).}

\item{models}{The model bundle returned by
\code{\link[=train_weights]{train_weights()}} with \code{return_models = TRUE} (the \code{models} element).}

\item{margin}{Cells whose top-minus-second score (\code{margins}) is below
\code{margin} are assigned \code{"Unknown"} instead of their top cell type.
\code{0} (default) assigns every cell.}
}
\value{
A list with:
\describe{
\item{\code{scores}}{A matrix (cells x cell types) of the linear
predictors, with the cell types in the order of the model bundle.}
\item{\code{assignments}}{A named vector with the top-scoring cell type
of every cell (or \code{"Unknown"} for cells below the \code{margin}).}
\item{\code{margins}}{A named numeric vector with the top-minus-second
score of every cell.}
}
}
\description{
Scores each cell with the linear predictors of the per-cell-type models
persisted by \code{\link[=train_weights]{train_weights()}} with \code{return_models = TRUE} (methods
\code{"glmnet"} and \code{"lr"} only): for a cell with expression \code{x} of the model
features, the score of cell type \code{t} is
\verb{eta_t = (Intercept)_t + sum_f coef_tf * (x_f - center_f) / scale_f},
where \code{center}/\code{scale} are the per-gene centering/scaling recorded when
the models were trained. Genes of the model features missing from
\code{exprs} contribute 0; extra genes of \code{exprs} are ignored.
}
\details{
The centering/scaling is folded into the coefficients, so a sparse
\code{exprs} matrix is never densified by the \code{(x - center) / scale} shift:
\verb{eta_t = (Intercept)_t - sum_f b_tf * center_f + sum_f b_tf * x_f} with
\code{b_tf = coef_tf / scale_f}.
}
