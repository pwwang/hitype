% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train_weights.R
\name{train_weights}
\alias{train_weights}
\title{Train weights for the markers}
\usage{
train_weights(
  path_to_gs,
  exprs,
  level = 1,
  scaled = FALSE,
  clusters = NULL,
  range = c(-5, -1, 1, 5),
  data_split = c(0.7, 0.2, 0.1),
  epochs = 20,
  batch_size = 32,
  run_weights_on_test = TRUE,
  cv_folds = 1,
  method = c("glmnet", "lr", "rf", "xgb", "lrp", "correlation", "uniform"),
  format = c("universal", "db"),
  drop_zero = TRUE,
  pos_only = FALSE,
  return_models = FALSE,
  seed = 8525
)
}
\arguments{
\item{path_to_gs}{Path to the gene set file without weights. The
training cell types are the cell types of the cells (see \code{clusters}):
a cell type in the marker file with cells of that type in the data is
trained on its own markers in the file. A cell type in the marker file
with no cells of that type in the data is ignored with a warning, and
its markers are pooled for the cell types in the data that are not
covered by the marker file (each of them is trained on the pooled
markers). Cell types in the data that are neither covered by the marker
file nor by pooled markers are not trained (with a warning).}

\item{exprs}{The expression matrix, or a seurat object
(rows: genes, columns: samples/cells)}

\item{level}{The level of the gene sets to train weights for if
you have multiple levels of gene sets.}

\item{scaled}{Whether the expression matrix is scaled}

\item{clusters}{The cell types of the cells. When \code{exprs} is a Seurat
object, it can be \code{NULL} (default) to take the cell types from
\code{Seurat::Idents()}, or a column name in the \code{meta.data} of the Seurat
object that holds the cell type of each cell. When \code{exprs} is a matrix,
it should be a named vector of cell types (names - cell names).}

\item{range}{The quantization range for the db output only (\code{format = "db"}): the 4-element form \code{c(-low, -high, low, high)} (default
\code{c(-5, -1, 1, 5)}) scales the positive weights into \verb{[low, high]} and
the negative ones into \verb{[-high, -low]} before rounding them to
integers, so genuinely positive markers always stay positive. The
universal output (default) keeps the raw trained weights as-is.}

\item{data_split}{A vector of fractions for training, validation and
testing. If only two fractions are provided, no testing set will be used.}

\item{epochs}{The number of epochs to train (lrp method only)}

\item{batch_size}{The batch size (lrp method only)}

\item{run_weights_on_test}{Whether to run the weights on the test set.
Requires that \code{data_split} has three elements.}

\item{cv_folds}{Number of cross-validation folds for weight estimation.
When > 1, weights are averaged across folds for stability.
Default is 1 (no cross-validation). Used by lr, glmnet, and lrp methods.}

\item{method}{The weight learning method. One of:
\describe{
\item{"uniform"}{All markers get equal weight (= 1). Fast baseline.}
\item{"correlation"}{Pearson correlation between each marker and
the binary cluster indicator. Simple, interpretable.}
\item{"lr"}{Logistic regression coefficients (one-vs-rest).
Classic ML approach. Use cv_folds for stability.}
\item{"glmnet"}{Sparse logistic regression with elastic net penalty
(alpha = 0.5). Automatically zeros out uninformative markers.
Recommended for most users. Requires the glmnet package.}
\item{"rf"}{Random forest permutation importance. Captures non-linear
marker interactions. Requires the ranger package.}
\item{"xgb"}{XGBoost gain-based feature importance.
State-of-the-art tree method. Requires the xgboost package.}
\item{"lrp"}{Neural network + Layer-wise Relevance Propagation.
Deep learning approach. Requires the keras and innsight packages.}
}}

\item{format}{The format of the output data frame. One of
\code{"universal"} (default) or \code{"db"} (the hitype/ScType wide format).}

\item{drop_zero}{Whether to drop markers whose trained weight is exactly
zero from the output (default \code{TRUE}). Such markers carry no learned
direction — with the glmnet method (coefficients at \code{lambda.1se}) most
candidate markers are zeroed. Dropping them makes zero mean "no
direction": the marker is absent from the cell type's list. Pass
\code{FALSE} to keep every candidate marker (with \code{format = "db"} they are
then encoded as \code{*}).}

\item{pos_only}{Whether to keep only the markers with a positive
trained weight in the output (default \code{FALSE}). A marker can be trained
with a negative weight (anti-correlating with the cell type, e.g. by
the \code{"correlation"} method); such rows are reported with
\code{direction = "negative"} and are dropped when \code{pos_only = TRUE},
so the returned table contains markers overexpressed in each cell type
only.}

\item{return_models}{Whether to also return the fitted per-cell-type
prediction models (default \code{FALSE}). With \code{FALSE} (default) the return
value is unchanged: a data frame with the weights. With \code{TRUE}, a list
is returned instead:
\describe{
\item{\code{weights}}{The usual weights data frame (universal or db
format).}
\item{\code{models}}{A model bundle list that \code{\link[=hitype_score_models]{hitype_score_models()}}
can score new cells with:
\describe{
\item{\code{method}}{The weight learning method.}
\item{\code{level}}{The level of the gene sets.}
\item{\code{features}}{The ordered gene list the models were fit on.}
\item{\code{center}, \code{scale}}{Numeric vectors named by \code{features}
with the per-gene centering/scaling that was applied to the
training matrix, so scoring can reproduce it exactly.}
\item{\code{coefs}}{A named list, one entry per trained cell type,
each a named numeric coefficient vector of the linear
predictor including the intercept as \code{"(Intercept)"}. \code{NULL}
for methods without linear models.}
}}
\item{\code{labels}}{The trained cell types (names of \code{coefs}).}
}
Model bundles are only produced for the linear-model methods
\code{"glmnet"} and \code{"lr"}; with any other method \code{coefs} is \code{NULL},
\code{labels} is \code{NULL}, and \code{\link[=hitype_score_models]{hitype_score_models()}} errors.}

\item{seed}{Random seed for reproducibility}
}
\value{
By default, a data frame with the weights in the universal marker
format (default) or the db format (\code{format = "db"}), that can be used
directly by \code{\link[=gs_prepare]{gs_prepare()}}. With \code{return_models = TRUE}, a list with the
weights data frame, the model bundle and the trained cell type labels
(see \code{return_models}).
}
\description{
Train weights for the markers
}
