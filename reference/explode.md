# Split the string by the separator and trim the whitespaces.

Split the string by the separator and trim the whitespaces.

## Usage

``` r
explode(x, sep = ",")
```

## Arguments

- x:

  A string.

- sep:

  The separator.

## Value

A character vector.
