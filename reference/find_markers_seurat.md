# Find markers with Seurat FindAllMarkers / FindMarkers

Find markers with Seurat FindAllMarkers / FindMarkers

## Usage

``` r
find_markers_seurat(
  exprs,
  clusters,
  top,
  min_log2fc,
  min_pct,
  pos_only,
  refs,
  max_pct_out
)
```
