# Find markers with Seurat FindAllMarkers

Find markers with Seurat FindAllMarkers

## Usage

``` r
find_markers_seurat(
  exprs,
  clusters,
  top,
  min_log2fc,
  min_pct,
  only_pos,
  include_negative
)
```
