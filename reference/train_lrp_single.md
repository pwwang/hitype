# Train a single LRP model (helper)

Train a single LRP model (helper)

## Usage

``` r
train_lrp_single(
  z,
  clusters,
  uclusters,
  train_idx,
  test_idx,
  epochs,
  batch_size,
  data_split
)
```
